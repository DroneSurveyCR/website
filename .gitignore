# Temp / crawl files
dronesurveycr/
fetched_page.html

# Claude local settings (contains personal config)
.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
*.swp
*.swo

# Env / secrets
.env
.env.local

# Logs
*.log
.vercel
service-account-key.json

# Media bridge inbox + raw podcast media (never commit media files)
inbox/
*.m4a
*.mp4
*.mp3
*.wav
automation/install_log.txt

# Exception: site hero/section videos (NOT podcast media) must be deployed
!images/*.mp4

# NEVER commit credentials (leaked 2026-06; rotated)
google-oauth-credentials.json
drone-survey-costa-rica-036c0797c890.json
*.service-account.json
*.bak-*

# Internal dashboards — repo root is the public web root, keep these out
Partner CRM/
command-center-updated.html
